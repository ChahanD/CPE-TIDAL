<nav id="navbar" class="navbar navbar-expand {{ navbarColor }}">
    <a class="navbar-brand ps-3" href="../index.php">
        <img src="images/logo.webp" width="30" height="30" class="d-inline-block align-top" alt=""/>
    </a>

    <div class="collapse navbar-collapse d-flex" id="navbarNav">
        <ul class="navbar-nav">
            {% if currentPage != 'index' %}
                <li class="nav-item active">
                    <a class="nav-link" href="../index">Accueil</a>
                </li>
            {% endif %}

            {% if currentPage != 'patho' %}
                <li class="nav-item active">
                    <a class="nav-link" href="../pathologie">Pathologies</a>
                </li>
            {% endif %}

            {% if currentPage != 'sympto' %}
                <li class="nav-item active">
                    <a class="nav-link" href="../symptome">Symptômes</a>
                </li>
            {% endif %}
        </ul>

        {% if currentPage != 'authentication' %}
            <ul class="navbar-nav ms-auto pe-3">
                <li class="nav-item active">
                    <a class="nav-link" href="../authentication">Authentification</a>
                </li>
            </ul>
        {% endif %}
    </div>
</nav>
